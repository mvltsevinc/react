import React from 'react';

const validation = (props) => {
    return (
        <div>
            <p>{props.textLength}</p>
            <p>{props.mesaj}</p>
        </div>
    );
};

export default validation;